.terraform/
*.tfstate
*.tfstate.*

.DS_Store
.tfvars

# Local, non-committed values (keep personal/secret config out of the public repo)
*.auto.tfvars
vars.local.yml
# Filled-in EC2 user-data (contains the real SSH port); paste-time copy only
aws/user-data.local.yaml

# Generated by Terraform (see hosts.tftpl / ssh_config.tftpl)
hosts
ssh_config

# Claude
.claude/*

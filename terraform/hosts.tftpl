# Ansible inventory generated by Terraform (local_file.ansible_inventory) on apply.
# Do not edit by hand; changes are overwritten. Connection details (port, user,
# key) come from group_vars/all.yml, vars.local.yml, and the playbooks.
%{ for name, ip in servers ~}
${name} ansible_host=${ip}
%{ endfor ~}
